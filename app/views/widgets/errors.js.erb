<% if flash[:alert] %>
  alert("<%= raw escape_javascript( flash[:alert] )%>")
<% end %>
<% if @widget.errors.size.nonzero? %>
  alert("<%= raw escape_javascript( @widget.errors.full_messages.map{|m| "* #{m}\n"}.join )%>")
<% end %>